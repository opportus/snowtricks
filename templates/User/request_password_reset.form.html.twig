{% extends 'User/layout.form.html.twig' %}
{% block user_content %}
    {{ parent() }}
    <h1>{{ 'user.request_password_reset.form.title'|trans }}</h1>
    {{ form_start(form) }}
        {{ form_errors(form) }}
    {{ form_end(form) }}
{% endblock user_content %}
